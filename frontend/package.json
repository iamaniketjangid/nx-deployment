{
  "name": "frontend",
  "version": "1.0.0",
  "private": true,
  "description": "Frontend app for CI/CD build comparison (without Nx)",
  "main": "index.js",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "echo '🚀 Frontend build started' && sleep 5 && echo '✅ Frontend build completed'",
    "test": "echo '🧪 Frontend tests running' && sleep 2 && echo '✅ Frontend tests passed'"
  },
  "keywords": ["frontend", "ci", "pipeline"],
  "author": "Aniket",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {}
}
