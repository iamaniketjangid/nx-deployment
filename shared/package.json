{
  "name": "shared-lib",
  "version": "1.0.0",
  "private": true,
  "description": "Shared library used by frontend and backend",
  "main": "index.js",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "echo '🚀 Shared lib build started' && sleep 4 && echo '✅ Shared lib build completed'"
  },
  "keywords": ["shared", "library", "ci"],
  "author": "Aniket",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {}
}
